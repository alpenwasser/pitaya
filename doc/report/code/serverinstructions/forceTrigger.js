// Request

{
    "forceTrigger": "true"
}

// Response

{
    "response", {
        {"request", "forceTrigger"},
        {"status", status}, // "error" or "ok"
        {"error", errorMessage}
    }
}